requests>=2.28
pandas>=1.5
geopandas>=0.12
shapely>=2.0
numpy>=1.23
fuzzywuzzy>=0.18
osmnx>=1.3

# Notes:
# - `geopandas` pulls in several geospatial libs (fiona, pyproj). On many systems
#   these require system packages (GDAL, PROJ). If installation fails, install
#   system dependencies first or use conda: `conda install -c conda-forge geopandas`.
# - Tests use the standard library `unittest`, so no additional test runner is required.
# Optional (performance) - install python-Levenshtein to speed up fuzzywuzzy:
# python-Levenshtein>=0.20
# Note: `osmnx` depends on geospatial stack (geopandas, networkx, shapely, pyproj).
# On many systems it's easiest to install the geospatial stack via conda-forge:
#   conda install -c conda-forge osmnxpython-Levenshtein
